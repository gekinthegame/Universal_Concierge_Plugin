[toolchain]
channel = "1.95.0"
profile = "minimal"
# clippy/rustfmt are intentionally NOT requested here. CI installs them explicitly via
# its toolchain action; requesting them here made the release job (which needs neither)
# auto-install rustfmt and collide with the runner's existing cargo-fmt.
